@model ERP.Project.Data.Models.ItemPriority
@using ERP.Project.Data.Models;

@{
    ViewBag.Title = "Edit";
}

<h2>Edit</h2>

<div>
    @using (Html.BeginForm("Edit", "ItemPriority"))
    {
        <table>
            <tr>
                <td>Item Priority Id</td>
                <td>@Html.TextBoxFor(m => m.PriorityId, new { @class = "form-control", @readonly = "null" })</td>
                
            </tr>
            <tr>
                <td>Color Code</td>
                <td>@Html.DropDownList("ColorCode", (IEnumerable<SelectListItem>)ViewBag.ColorCode, "--Color Code-- ", new { @class = "form-control" })</td>
            </tr>
            <tr>
                <td>Item Priority Name</td>
                <td>@Html.TextBoxFor(m=>m.PriorityName,new { @class = "form-control" })</td>
            </tr>
        </table>
        <input class="btn btn-success" type="submit" value="Update" />
    }
</div>